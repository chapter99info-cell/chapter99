/** Live Supabase table names (singular, lowercase) — must match database exactly */
export const AGENCY_TABLES = {
  client: 'client',
  project: 'project',
  task: 'task',
  billing: 'billing',
  prompts: 'prompts',
} as const
